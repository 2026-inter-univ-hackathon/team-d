# （プロダクト名をここに書く）

> **14:45 に最初にやること。** 14:15 と 14:45 に Slack へ投稿した内容を、下の「これは何か」と
> 「決めたこと」に**貼るだけ**です。書き直さなくて構いません。

## これは何か

（1行。誰の何を解決するか。**14:15 の投稿の①②**をそのまま）

---

## このプロジェクトの方針（2026-09-23 更新）

ユーザーの依頼により、Firebase・GitHub Pages構成へ変更します。

- 画面は既存の HTML / CSS / JavaScript を活かす。
- Firebase Authenticationで実際のメールアドレス、ユーザー名、パスワードを管理する。
- 予定はCloud Firestoreへユーザー別に保存し、本人以外のアクセスをSecurity Rulesで拒否する。
- Firebase App Checkで不正なクライアントからの通信を減らす。
- GitHub Pagesで静的画面を公開し、Django・SQLiteは使用しない。
- 予定のメール通知と定期送信処理は実装しない。
- ステータス変更は人が選んで確定する。
- 動作確認済みのステップごとにコミットする。DB・秘密情報・仮想環境は含めない。

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**: 未確定トレイへの予定追加・D&D配置、月表示／タイムライン表示、タイムラインでの所要時間・開始時刻ドラッグ変更（上下ハンドル判定拡大・15分刻み・終了時刻自動確定）、予定詳細編集、開始・終了時刻と所要時間の相互参照入力、24時間を超える予定・日付をまたぐ予定の月・タイムライン表示＆詳細編集、終了時刻未定時のタイムライン下部破線表示、トレイ予定の複数日時展開／毎週繰り返し展開、予定詳細から入力欄への読み込み、予定のお気に入り登録＆ワンクリック入力、お気に入り削除確認、直近の登録履歴サジェスト表示
- **今やりかけのこと**: 特になし（リサイズハンドル判定拡大および15分刻み対応完了）
- **次にやること**: 実際の利用シーンに合わせたUIの微調整や使い勝手の検証
- **ハマっていること**: 特になし

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- （例）保存するデータは `localStorage` のキー `app-data` にまとめる
- （例）画面は `index.html` の1枚だけにする
- （例）今日は通知機能を作らない
- お気に入り予定のデータは `localStorage` のキー `tentative_calendar_favorites` に保存する
- 登録履歴データは `localStorage` のキー `tentative_calendar_history` に保存する
- お気に入り削除時には `confirm` ダイアログで確認メッセージを表示して誤削除を防ぐ
- 複数日時・毎週繰り返しの作成時も、ステータス（未確定/確定）をユーザーが選べるようにし、人の判断を介在させる動線を保つ
- トレイの予定詳細からも直接日付・時間帯を指定してカレンダーへ配置できるようにし、さらに複数日時や繰り返しへの一括展開や入力欄への読み込みも提供する
- 終了時刻と開始時刻・終了日は自動連動させ、終了時刻未定の場合はタイムライン表示で下部を破線＋「〜未定」表示とし、下端ドラッグで終了時刻を確定できるようにする
- 24時間を超える長期予定（所要時間の上限なし）および夜間〜早朝等の日付をまたぐ予定（例: 22:00〜翌日02:00）、15分刻み等の分単位予定に対応。予定詳細では「開始日」「開始時刻」「終了日」「終了時刻」で直感的に指定可能とし、所要時間は開始・終了の日時差分から自動算出＆プレビュー表示する。Firestore Rules・クライアントバリデーション・月表示（該当日セル全表示）・タイムライン表示（当日区間表示＋継続バッジ）を完備する
- 予定チップのステータス別表示はBeer CSSのユーティリティクラス（未確定: `fill`, 確定: `secondary`, 完了: `surface-container`）を使用し、CSSの直接的な背景色指定ではなくテーマ連動の配色設計とする

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**:
- **その理由**:
