# （プロダクト名をここに書く）

> **14:45 に最初にやること。** 14:15 と 14:45 に Slack へ投稿した内容を、下の「これは何か」と
> 「決めたこと」に**貼るだけ**です。書き直さなくて構いません。

## これは何か

（1行。誰の何を解決するか。**14:15 の投稿の①②**をそのまま）

---

## このプロジェクトの方針（2026-09-23 更新）

ユーザーの依頼により、Firebase・GitHub Pages構成へ変更します。

- 画面は既存の HTML / CSS / JavaScript を活かす。
- Firebase Authenticationで実際のメールアドレス、ユーザー名、パスワードを管理する。
- 予定はCloud Firestoreへユーザー別に保存し、本人以外のアクセスをSecurity Rulesで拒否する。
- Firebase App Checkで不正なクライアントからの通信を減らす。
- GitHub Pagesで静的画面を公開し、Django・SQLiteは使用しない。
- 予定のメール通知と定期送信処理は実装しない。
- ステータス変更は人が選んで確定する。
- 動作確認済みのステップごとにコミットする。DB・秘密情報・仮想環境は含めない。

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**:
- **今やりかけのこと**:
- **次にやること**:
- **ハマっていること**:

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- （例）保存するデータは `localStorage` のキー `app-data` にまとめる
- （例）画面は `index.html` の1枚だけにする
- （例）今日は通知機能を作らない

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**:
- **その理由**:
